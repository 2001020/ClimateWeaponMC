package com.stormweapon.item;

import net.minecraft.world.item.Item;

/** Launcher payload that detonates into a fog weapon deployment instead of a storm. */
public final class FogMissileItem extends Item {
    public FogMissileItem(Properties properties) {
        super(properties);
    }
}
