package com.stormweapon.item;

import net.minecraft.world.item.Item;

/** A real launcher payload; it is consumed only after the launcher accepts the load. */
public final class WeatherMissileItem extends Item {
    public WeatherMissileItem(Properties properties) {
        super(properties);
    }
}
